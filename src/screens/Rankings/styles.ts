import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
  },
  image: {
    height: 150,
    resizeMode: 'contain',
  },
  rankings: {
    fontWeight: 'bold',
    color: 'white',
    fontSize: 24,
    marginBottom: 16,
  },
})

export default styles
